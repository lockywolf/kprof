#MIN_CONFIG(3.0.0)

AM_INIT_AUTOMAKE(kprof, 1.4.3)
AC_C_BIGENDIAN
AC_CHECK_KDEMAXPATHLEN

dnl ------------------
dnl Check for QTreeMap
dnl ------------------

AC_CHECK_LIB(qtreemap, main, [LIBQTREEMAP="-lqtreemap"])
AC_SUBST(LIBQTREEMAP)
if test "$LIBQTREEMAP" != ""; then
	AC_DEFINE(HAVE_QTREEMAP,1,[Define if you have the libqtreemap installed])
fi


